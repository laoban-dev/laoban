{
  "name": "${projectDetails.name}",
  "description": "${projectDetails.description}",
  "version": "${version}",
  ${include(https://raw.githubusercontent.com/phil-rice/laoban/master/common/templates/typescript_405/package.json.dependencies.template)
}