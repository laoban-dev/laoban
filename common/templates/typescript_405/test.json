{
  "name": "${projectDetails.name}",
  "description": "${projectDetails.description}",
  "version": "${version}",
  "links": "${projectDetails.details.link:map<<>>(i:\"<<i>>\":\"<<version>>\")}"

}