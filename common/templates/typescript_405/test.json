{
  "name": "${projectDetails.name}",
  "description": "${projectDetails.description}",
  "version": "${version}",
  "dependencies": {${projectDetails.details.links:map<<>>(i=>"<<i>>":"<<version>>")}}
}